<resources>

    <!-- Base application theme. -->
    <style name="Theme.Primary.Eculture" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="Button.Tourist" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/touristcolor</item>
    </style>

    <style name="Button.Curator" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/curatorcolor</item>
    </style>

    <style name="customFab">
        <item name = "android:background">@drawable/fab_label_background</item>
        <item name = "android:textColor">@color/white</item>
    </style>

</resources>
